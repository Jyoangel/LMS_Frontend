/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['s.gravatar.com', 'cdn.auth0.com', 'res.cloudinary.com'], // Add any other domains you need
    },
};

export default nextConfig;
